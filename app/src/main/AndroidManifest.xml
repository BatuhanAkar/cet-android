<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <uses-permission android:name="android.permission.CAMERA"/>

    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hoşbeş"
        android:usesCleartextTraffic="true"
        tools:targetApi="31" >


        <activity
            android:name=".views.RandomActivity"
            android:theme="@style/JitsiMeetActivityStyle"
            android:exported="false" />

        <activity
            android:name=".views.InCallActivity"
            android:theme="@style/JitsiMeetActivityStyle"
            android:exported="false" />

        <activity
            android:name=".views.OutCallActivity"
            android:theme="@style/JitsiMeetActivityStyle"
            android:exported="false" />

        <activity
            android:name=".views.GiphyActivity"
            android:theme="@style/JitsiMeetActivityStyle"
            android:exported="false" />

        <activity
            android:name=".views.VideoChannel"
            android:theme="@style/JitsiMeetActivityStyle"
            android:exported="false" />

        <service android:name=".utility.SessionService" />


        <activity
            android:name=".views.VoiceChannel"
            android:theme="@style/JitsiMeetActivityStyle"
            android:exported="false" />

        <activity android:name=".views.WhisperChatActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:exported="false"/>
        <activity android:name=".views.RandomConnectionActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:exported="false"/>


        <service android:name=".utility.SessionService" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:windowSoftInputMode="adjustResize" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>